server:
  port: 8083

spring:
  application:
    name: restaurant-menu-service

  datasource:
    url: jdbc:mysql://localhost:3306/menu_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
