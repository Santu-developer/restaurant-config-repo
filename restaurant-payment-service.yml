server:
  port: 8086

spring:
  application:
    name: restaurant-payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
